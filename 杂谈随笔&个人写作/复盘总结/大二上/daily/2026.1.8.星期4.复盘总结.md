# 2026.1.8.星期4.复盘总结

## （一）一些反思
1. 昨天看完了《银河帝国6：基地边缘》，很好看，看完后几小时脑子都还在围绕这个话题打转  

2. 但是，埋到看书里面、一次性看完某本书，并不是我的原意——我本想趁着[刚写不久的博客](/杂谈随笔&个人写作/读书笔记/2025)还是热乎的，继续把[2025.12月总结](/杂谈随笔&个人写作/复盘总结/大二上/monthly/软工.大二上.12月总结.md)一并拿下，然而我还是有一些害怕，于是就有了看书的逃避行为  
到昨天晚上，我突然反应过来，我的思维依旧是“线性”的：必须按顺序一样一样的完成目标。然而这种思维的弱点是**一旦在某个地方卡住，久久不能推进，便会产生强大的挫折**，甚至引起反感/恐惧进而逃避。  

3. 然而，我可以**先做其他想做的事情**啊——很简单直接的思路，但在我跟自己杠起来的时候确完全没想到——固定的“某个时间段应该做什么”并不是不好的计划，但我也可以这段时间做想做的一件事情，另一段时间又做另一个想做的事情，全凭感觉、体验，可能并不“规律”、“均匀”，但这种“一时兴起”地做事，如果能够合理的组织起来，反而应该可以提高整体推进的效率——因为始终体验良好、并且灵活，**并不需要先把这个做完再做那个**



## （二）继续学SQL
### 1、对表格结构的操作
#### （1）增
```
# 添加字段
`alter table 表名 add 添加的字段名 字段类型 [约束] [comment注释];`

# 理解记忆：修改了表格的组成部分（结构） => 需要“改变”表格 => 用 `alter table` 开头
# 这些指令都会先在开头标记“大方向要干什么”（`alter/create/drop table`），之后再补充细节
# 约束：可不可以为空 `null/not null`、`default`默认值、`unique`、`primary key`
# 中括号 [] 包裹的内容可加可不加
# 大部分情况，[约束]  [comment] 的顺序可换

# 创建表
create table [if not exists] 表名 (
	各种字段
) [comment注释] ;
```

#### （2）删
```
# 删除字段；add/drop = add/drop column（毕竟确实增删字段看起来就是增删了1列）
alter table 表名 drop 要删的字段名;

# 删除表
drop table [if exists] 要删除的表名;
```

#### （3）改
```
# modify不改名，只“修改”字段的属性（类型、约束、注释）
`alter table 表名 modify 字段名 新的字段类型 [约束] [comment注释];`

# change“改变”整个字段（名称、类型）
`alter table 表名 change 字段名 新的字段名（也可以跟原来一样） 新的字段类型 [约束] [comment注释];`

# 重命名
`rename table 原来的表名 to 新的表名;`  =  alter table 原来的表名 rename to 新的表名
```

#### （4）查
```
# 展示当前数据库文件夹（`select database();`）的所有表格
`show tables;`

# 描述这个表 => 从它的组成结构来描述 => 展示表的结构
`desc 表名;`  =  describe 表名;`  ≈  `show [full] columns from 表名;`

# 查看创建表的源代码/SQL语句
`show create table 表名;`
```


### 2、对表格数据的操作
#### （1）增
```
# 添加1行数据
insert into 表名 ( 字段1的名字, 字段2的名字... ) values( 字段1的值, 字段2的值... );
insert into 表名 values( 所有字段的值 );
# ①类似调用构造器创建对象
# ②如果字段有默认值，可以不用添加这个字段的数据
# ③所有字段都没有默认值、需要手动初始化时，有点像全参构造器

# 添加多行数据
insert into 表名 ( 字段1的名字, 字段2的名字... ) values( 字段1的值, 字段2的值... ), ( 字段1的值, 字段2的值... )...;
```

#### （2）删
```
# 删除指定行
delete from 表名 [ where 条件 ];

# 删除 id = 1 的那行
delete from user_table where id = 1; 

# 删除所有行
delete from 表名;
```

#### （3）改
```
# 修改指定的行（**不加 [where 条件] 则修改所有行**）
update 表名 set 字段1的名字 = 数据, 字段2的名字 = 数据...[where 条件];
update user_table set username = '老王', age = 66 where id = 1;
```

#### （4）查
还在学📕~~~


### 3、操作经验
（1）数据库操作/使用场景下，通常用小写字母 + 下划线（蛇形）命名法，比如`user_table`而不是`userTable`、`UserTable`  

（2）关于时间的函数   
①`datetime`类型：`now()`获取当前的日期、时间  
②`date`类型：`curdate()`  =  `current_date()`、`date( now() )` 截取日期部分  
③`time`类型：`curtime()`  =  `current_time()`、`time( now() )` 截取时间部分  
④注意：`date`/`time` 类型的字段，**不能用函数设置默认值( default )**，但可以在 `insert`/`update` 时调用函数  

（3）SQL语句可以拆成多行来写，看起来更舒服、结构清晰  
```
alter table user_table 
modify username
varchar( 8 )
not null
default '神秘用户' 
comment '用户名( <= 8 chars )';
```

---



